package com.earthtrip.notification.application.port.out;import java.time.*;import java.util.*;
public interface NotificationStorePort{List<NotificationRecord>list(UUID user);Optional<NotificationRecord>find(UUID id);NotificationRecord save(NotificationRecord r);Optional<PreferenceRecord>preference(UUID user);PreferenceRecord savePreference(PreferenceRecord r);void saveDevice(DeviceRecord r);Optional<DeviceRecord>device(String id);record NotificationRecord(UUID id,UUID userId,UUID tripId,String type,String title,String body,String deepLink,Map<String,Object>metadata,Instant createdAt,Instant readAt,Instant hiddenAt,long version){}record PreferenceRecord(UUID userId,boolean mentions,boolean schedule,boolean expense,boolean invitation,boolean push,boolean email,LocalTime quietStart,LocalTime quietEnd,String quietTimeZone,long version,Instant updatedAt){}record DeviceRecord(String deviceId,UUID userId,String platform,String tokenHash,String tokenCipher,int appBuild,boolean active,Instant createdAt,Instant updatedAt){}}
